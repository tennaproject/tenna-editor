import { StoryChapterLayout, StoryChapterSections } from '@components';

export function StoryChapter4() {
  return (
    <StoryChapterLayout chapter={4}>
      <StoryChapterSections chapter={4} />
    </StoryChapterLayout>
  );
}
